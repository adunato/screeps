var defines = {

    initDefines: function () {
        let MIN_UPGRADERS = 2;
        let MIN_BUILDERS = 3;
        let MIN_HARVESTERS = 7;
        let MIN_NOMAD_HARVESTERS = 5;
    }

}

module.exports = defines;